\section*{Приложение А}
\addcontentsline{toc}{chapter}{Приложение А. Программный код разработанной ФИ НС-ПСПС}

\begin{lstlisting}[
	language={[ISO]python},
	caption={Программный код модели \texttt{mamba\_pinn\_rpm}},
	label={lst:mamba_pinn_rpm},
	breaklines=true,
	breakatwhitespace=true,
	breakautoindent=true,
	basicstyle=\footnotesize\ttfamily,
	keywordstyle=\bfseries\color{blue!70!black},
	commentstyle=\itshape\color{green!50!black},
	stringstyle=\color{red!70!black},
	frame=single,
	framerule=0.4pt,
	framesep=2pt,
	xleftmargin=1.5em,
	numbers=left,
	numberstyle=\tiny\color{gray},
	numbersep=0.8em,
	tabsize=4,
	captionpos=b
	]
	class MambaRPMPINN(nn.Module):
	"""MambaRPMPINN — полная RPM-informed Physics-Informed Neural Network"""
	def __init__(self, input_dim=17, d_model=192, d_state=16, d_conv=4, expand=2, num_layers=3):
	super().__init__()
	self.embedding = nn.Linear(input_dim, d_model)
	nn.init.kaiming_normal_(self.embedding.weight)
	self.norm_in = nn.LayerNorm(d_model)
	
	self.mamba_layers = nn.ModuleList([
	Mamba(d_model=d_model, d_state=d_state, d_conv=d_conv, expand=expand)
	for _ in range(num_layers)
	])
	
	self.norm_out = nn.LayerNorm(d_model)
	self.fc_vel = nn.Linear(d_model, 3)
	self.fc_quat = nn.Linear(d_model, 4)
	self.fc_accel = nn.Linear(d_model, 3)
	self.fc_torque_inter = nn.Linear(d_model, 4)
	
	# Физические параметры (регистрируем как буферы)
	self.register_buffer('g', torch.tensor(9.81))
	self.register_buffer('m', torch.tensor(2.0))
	self.register_buffer('l', torch.tensor(0.174))
	self.register_buffer('k', torch.tensor(2.32e-8))
	self.register_buffer('b', torch.tensor(1.1e-8))
	self.b_drag = nn.Parameter(torch.tensor(0.02))
	
	self.register_buffer('I_xx', torch.tensor(0.02167))
	self.register_buffer('I_yy', torch.tensor(0.02167))
	self.register_buffer('I_zz', torch.tensor(0.04000))
	self.register_buffer('coeff_fz', torch.tensor([1., 1., 1., 1.]))
	
	self.register_buffer('accel_mean', torch.tensor([0.0, 0.0, -9.81]))
	self.register_buffer('accel_std', torch.tensor([0.5, 0.5, 1.0]))
	
	l_arm = self.l * math.sqrt(2.0)
	self.register_buffer('coeff_tx', torch.tensor([-1., 1., 1., -1.]) * l_arm)
	self.register_buffer('coeff_ty', torch.tensor([-1., -1., 1., 1.]) * l_arm)
	self.register_buffer('coeff_tz', torch.tensor([-1., 1., -1., 1.]) * (self.b / self.k))
	
	def forward(self, x):
	x = self.embedding(x)
	x = self.norm_in(x)
	
	residual = x
	for mamba in self.mamba_layers:
	x = mamba(x) + residual
	residual = x
	
	x = self.norm_out(x)
	
	vel = self.fc_vel(x)
	quat = self.fc_quat(x)
	accel_nn = self.fc_accel(x)
	torque_inter = self.fc_torque_inter(x)
	
	# Нормализация кватерниона
	quat_norm = torch.norm(quat, dim=-1, keepdim=True)
	quat = quat / torch.clamp(quat_norm, min=1e-6)
	quat = torch.clamp(quat, -1.0, 1.0)
	
	pred = torch.cat([vel, quat, accel_nn], dim=-1)
	return pred, torque_inter
\end{lstlisting}

\section*{Приложение Б}
\addcontentsline{toc}{chapter}{Приложение Б. Архитектура потока данных восстановления траектории}

\section{Архитектура потока данных восстановления траектории}

На рисунке \ref{fig:data_flow} представлен полный поток данных с датчиков ГНСС, БИНС, оборотов винтов
в задаче восстановления траектории с применением ФИ НС-ПСПС. 
\begin{figure}[htbp]
	\centering
	\resizebox{0.92\linewidth}{!}{\input{images/fig_dataflow.tex}}
	\caption{Архитектура потока данных восстановления траектории.}
	\label{fig:data_flow}
\end{figure}
